{
  "name": "grasshopper-website",
  "private": true,
  "version": "1.0.0",
  "description": "Static build tooling for grasshoppersolutions.online. The site itself is plain HTML/CSS/JS with no runtime build step; this only compiles the Tailwind stylesheet ahead of time so visitors never download the CDN compiler.",
  "scripts": {
    "build:css": "tailwindcss -i ./styles/tailwind-input.css -o ./styles/tailwind.css --minify",
    "watch:css": "tailwindcss -i ./styles/tailwind-input.css -o ./styles/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.19"
  }
}
